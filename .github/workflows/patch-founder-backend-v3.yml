name: Patch founder backend v3

# Retired automation: kept only for historical/manual recovery.
# It must not mutate production backend automatically on pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo 'Patch founder backend v3 is retired; use the reviewed RBAC implementation in backend/server.js.'
