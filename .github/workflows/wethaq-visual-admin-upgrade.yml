name: Wethaq Visual Identity & Admin Upgrade

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy visual admin upgrade disabled; canonical production gate owns source changes and release verification.'
