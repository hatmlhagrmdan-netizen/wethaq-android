name: Wethaq Production Candidate Gate

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy production candidate workflow disabled; use Wethaq Canonical Production Gate.'
