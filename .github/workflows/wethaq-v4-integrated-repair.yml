name: Wethaq V4 Integrated Repair

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply robust integrated repairs
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re

          def R(p): return Path(p).read_text(encoding='utf-8')
          def W(p,s): Path(p).write_text(s,encoding='utf-8')
          def replace_once(s, old, new, name):
              if old in s:
                  return s.replace(old,new,1)
              if new in s:
                  return s
              raise SystemExit('PATCH_MISSING: '+name)

          # HOME: paid-role notice + faster IO pool.
          p='app/src/main/java/com/wethaq/app/MainActivity.java'; s=R(p)
          s=replace_once(s,'private final ExecutorService io=Executors.newSingleThreadExecutor();','private final ExecutorService io=Executors.newFixedThreadPool(4);','io pool')
          s=replace_once(s,'content.addView(me,lp(-1,-2,12));menu("⌕  البحث",this::searchScreen);','content.addView(me,lp(-1,-2,12));addAdminPricingNotice();menu("⌕  البحث",this::searchScreen);','home notice')
          if 'private void addAdminPricingNotice()' not in s:
              s=s.replace('private void publicAdministrationScreen(){startActivity(new Intent(this,PublicAdministrationActivity.class));}', '''private void publicAdministrationScreen(){startActivity(new Intent(this,PublicAdministrationActivity.class));}
 private void addAdminPricingNotice(){LinearLayout card=new LinearLayout(this);card.setOrientation(LinearLayout.VERTICAL);card.setPadding(dp(14),dp(12),dp(14),dp(12));GradientDrawable gd=new GradientDrawable();gd.setColor(Color.rgb(28,24,12));gd.setCornerRadius(dp(16));gd.setStroke(dp(2),gold());card.setBackground(gd);TextView t=tv("💼 المناصب الإدارية مدفوعة",18,Color.WHITE);t.setGravity(Gravity.RIGHT);t.setTypeface(Typeface.DEFAULT,Typeface.BOLD);card.addView(t);TextView b=tv("المناصب الإدارية في وَثاق مدفوعة لمدة سنة واحدة.\\nللحصول على منصب إداري، يرجى التواصل مع المدير العام ومؤسس وَثاق.",15,gold());b.setGravity(Gravity.RIGHT);card.addView(b);content.addView(card,lp(-1,-2,14));}''',1)
          # Conversation: replace only method body using brace balancing, avoiding brittle anchors.
          start=s.find(' private void conversation(String id,String n){')
          if start<0: raise SystemExit('PATCH_MISSING: conversation method')
          brace=s.find('{',start); depth=0; end=None
          for i in range(brace,len(s)):
              if s[i]=='{': depth+=1
              elif s[i]=='}':
                  depth-=1
                  if depth==0: end=i+1; break
          if end is None: raise SystemExit('PATCH_MISSING: conversation closing brace')
          conv=''' private void conversation(String id,String n){stopPolling();activeId=id;activeName=n;base(n);LinearLayout header=new LinearLayout(this);header.setGravity(Gravity.CENTER_VERTICAL|Gravity.RIGHT);ImageView avatar=new ImageView(this);avatar.setImageDrawable(defaultAvatar());header.addView(avatar,new LinearLayout.LayoutParams(dp(58),dp(58)));LinearLayout names=new LinearLayout(this);names.setOrientation(LinearLayout.VERTICAL);names.addView(tv(n,19,Color.WHITE));names.addView(tv(id,13,Color.LTGRAY));header.addView(names,new LinearLayout.LayoutParams(0,-2,1));content.addView(header,lp(-1,-2,8));loadAvatarInto(id,avatar);Button video=btn("📹 مكالمة فيديو"),audio=btn("📞 مكالمة صوتية");content.addView(video,lp(-1,72,5));content.addView(audio,lp(-1,72,8));video.setOnClickListener(v->{click();startVideoCall();});audio.setOnClickListener(v->{click();startAudioCall();});messages=new LinearLayout(this);messages.setOrientation(LinearLayout.VERTICAL);ScrollView sc=new ScrollView(this);sc.setFillViewport(true);sc.addView(messages);content.addView(sc,new LinearLayout.LayoutParams(-1,0,1));LinearLayout composer=new LinearLayout(this);composer.setGravity(Gravity.CENTER_VERTICAL);Button mic=btn("🎙"),gallery=btn("▧"),send=btn("إرسال");input=field("اكتب رسالة…");composer.addView(mic,new LinearLayout.LayoutParams(dp(58),68));composer.addView(gallery,new LinearLayout.LayoutParams(dp(58),68));composer.addView(input,new LinearLayout.LayoutParams(0,68,1));composer.addView(send,new LinearLayout.LayoutParams(dp(86),68));content.addView(composer,lp(-1,-2,6));Button back=btn("‹ جهات الاتصال");content.addView(back,lp(-1,68,6));back.setOnClickListener(v->{click();contactsScreen();});send.setOnClickListener(v->{click();send();});mic.setOnClickListener(v->{click();if(recording)stopRecording(true);else startRecording();});gallery.setOnClickListener(v->{click();pickImage();});loadMessages();startPolling();}'''
          s=s[:start]+conv+s[end:]
          W(p,s)

          # PUBLIC ADMIN: replace show() only. Existing card(String,boolean) remains.
          p='app/src/main/java/com/wethaq/app/PublicAdministrationActivity.java';s=R(p)
          start=s.find('    private void show(String raw){')
          end=s.find('    private String nameOf',start)
          if start<0 or end<0: raise SystemExit('PATCH_MISSING: public admin show')
          show='''    private void show(String raw){try{JSONObject o=new JSONObject(raw);body.removeAllViews();body.addView(card("💼 المناصب الإدارية مدفوعة\\nتُمنح لمدة سنة واحدة من تاريخ التعيين — الأسعار سنوية.",true));JSONObject f=o.optJSONObject("founder");body.addView(card("👑 المؤسس\\n"+(f==null?"غير محدد":f.optString("name","غير محدد"))+"\\nالصلاحيات: الإشراف الكامل، التعيين والعزل، الحظر والتنبيه والإشراف على جميع المناصب.",true));JSONObject d=o.optJSONObject("deputies");body.addView(roleCard("🥇 النائب الأول","20$ / سنة","أوسع صلاحيات بعد المؤسس: الحظر والتنبيه وإدارة المستخدمين ضمن حدود الدور.",d==null?null:d.optJSONObject("deputy1")));body.addView(roleCard("🥈 النائب الثاني","17$ / سنة","الحظر والتنبيه للمستخدمين وأعضاء الإدارة دون تجاوز المؤسس والنائب الأول.",d==null?null:d.optJSONObject("deputy2")));body.addView(roleCard("🥉 النائب الثالث","12$ / سنة","الحظر والتنبيه الأساسي للمستخدمين وفق حدود الدور وعدم استهداف المناصب الأعلى.",d==null?null:d.optJSONObject("deputy3")));body.addView(text("👥 أعضاء الإدارة — حتى 10 أعضاء",20,Color.rgb(212,175,55)));JSONArray m=o.optJSONArray("members");for(int i=0;i<10;i++){JSONObject x=null;if(m!=null&&i<m.length())x=m.optJSONObject(i);body.addView(roleCard((i+1)+". عضو إدارة","8$ / سنة","المراقبة واستقبال البلاغات والتنبيه، دون تعيين أو الحظر العام.",x));}}catch(Exception e){status.setText("🔴 حدث خطأ أثناء قراءة البيانات");}}
    private LinearLayout roleCard(String role,String price,String permissions,JSONObject holder){LinearLayout c=new LinearLayout(this);c.setOrientation(LinearLayout.VERTICAL);c.setPadding(dp(16),dp(12),dp(16),dp(12));GradientDrawable d=new GradientDrawable();d.setColor(Color.rgb(24,24,28));d.setCornerRadius(dp(16));d.setStroke(dp(2),Color.rgb(70,70,75));c.setBackground(d);TextView a=text(role,19,Color.WHITE);a.setTypeface(Typeface.DEFAULT,Typeface.BOLD);TextView b=text(price,17,Color.rgb(212,175,55));b.setTypeface(Typeface.DEFAULT,Typeface.BOLD);TextView h=text("الصلاحيات: "+permissions,14,Color.LTGRAY);TextView u=text("المعين: "+(holder==null?"غير معين":holder.optString("name","غير معروف")),15,Color.WHITE);String exp=holder==null?"":holder.optString("expires_at","");TextView e=text(exp.isEmpty()?"مدة المنصب: سنة واحدة من تاريخ التعيين":"ينتهي في: "+exp,13,Color.LTGRAY);c.addView(a);c.addView(b);c.addView(h);c.addView(u);c.addView(e);LinearLayout.LayoutParams pp=new LinearLayout.LayoutParams(-1,-2);pp.bottomMargin=dp(10);c.setLayoutParams(pp);return c;}\n'''
          s=s[:start]+show+s[end:];W(p,s)

          # ADMIN CONSOLE: make target fields visible for all roles; preserve founder controls.
          p='app/src/main/java/com/wethaq/app/AdminActivity.java';s=R(p)
          marker='target=f("معرف وَثاق للمستخدم");minutes=f("مدة الحظر بالدقائق — 0 = نهائي");reason=f("السبب");if("founder".equals(role)){addFounderControls();addFounderModerationControls();}else if("deputy1".equals(role)||"deputy2".equals(role)||"deputy3".equals(role))addModerationControls();'
          if marker in s:
              s=s.replace(marker,'if("founder".equals(role)){addFounderControls();}addModerationTargetCard();if("founder".equals(role)||"deputy1".equals(role)||"deputy2".equals(role)||"deputy3".equals(role)||"admin_member".equals(role))addModerationControls();',1)
          else:
              s=re.sub(r'target=f\("معرف وَثاق للمستخدم"\);minutes=f\("مدة الحظر بالدقائق — 0 = نهائي"\);reason=f\("السبب"\);if\("founder"\.equals\(role\)\)\{addFounderControls\(\);addFounderModerationControls\(\);\}else if\("deputy1"\.equals\(role\)\|\|"deputy2"\.equals\(role\)\|\|"deputy3"\.equals\(role\)\)addModerationControls\(\);', 'if("founder".equals(role)){addFounderControls();}addModerationTargetCard();if("founder".equals(role)||"deputy1".equals(role)||"deputy2".equals(role)||"deputy3".equals(role)||"admin_member".equals(role))addModerationControls();', s, count=1)
          if 'private void addModerationTargetCard()' not in s:
              s=s.replace(' private void addModerationControls(){', ''' private void addModerationTargetCard(){LinearLayout box=card("🎯 إدارة مستخدم محدد");box.addView(heading("اكتب الاسم الكامل أو معرف وَثاق. الإجراء يطبق وفق صلاحيات رتبتك، واسم المؤسس محمي من الحظر والتنبيه."),lp(-1,-2,4));target=f("اسم المستخدم أو معرف وَثاق");minutes=f("مدة الحظر بالدقائق — 0 = نهائي");reason=f("سبب الحظر أو التنبيه");box.addView(target,lp(-1,60,6));box.addView(minutes,lp(-1,60,6));box.addView(reason,lp(-1,60,6));}\n private void addModerationControls(){''',1)
          s=s.replace('private void addModerationControls(){Button ban=b("🚫 حظر حسب الصلاحية"),alert=b("⚠️ إرسال تنبيه");','private void addModerationControls(){Button ban=b("🚫 حظر حسب الصلاحية"),alert=b("⚠️ إرسال تنبيه");if("admin_member".equals(role))ban.setEnabled(false);',1)
          W(p,s)

          # BACKEND RBAC + expiry + username resolution.
          p='backend/server.js';s=R(p)
          create_old="CREATE TABLE IF NOT EXISTS admin_roles(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL UNIQUE,role TEXT NOT NULL,admin_code_hash TEXT,created_by INTEGER,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_admin_roles_role ON admin_roles(role);"
          create_new="CREATE TABLE IF NOT EXISTS admin_roles(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL UNIQUE,role TEXT NOT NULL,admin_code_hash TEXT,created_by INTEGER,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,expires_at TEXT,FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_admin_roles_role ON admin_roles(role);"
          s=s.replace(create_old,create_new,1)
          if "ensureColumn('admin_roles','expires_at','TEXT');" not in s:
              idx="CREATE INDEX IF NOT EXISTS idx_admin_roles_role ON admin_roles(role);"
              if idx in s:s=s.replace(idx,idx+"ensureColumn('admin_roles','expires_at','TEXT');",1)
          role_old="function roleOf(userId){if(!userId)return null;const owner=db.prepare('SELECT id FROM users WHERE wethaq_id=?').get(OWNER_WETHAQ_ID);if(owner&&Number(userId)===Number(owner.id))return ROLE_FOUNDER;return db.prepare('SELECT role FROM admin_roles WHERE user_id=?').get(Number(userId))?.role||null}"
          role_new="function roleOf(userId){if(!userId)return null;const owner=db.prepare('SELECT id FROM users WHERE wethaq_id=?').get(OWNER_WETHAQ_ID);if(owner&&Number(userId)===Number(owner.id))return ROLE_FOUNDER;const row=db.prepare('SELECT role,expires_at FROM admin_roles WHERE user_id=?').get(Number(userId));if(!row)return null;if(row.expires_at&&new Date(row.expires_at)<=new Date())return null;return row.role||null}"
          s=s.replace(role_old,role_new,1)
          find_old="function findUser(id){return db.prepare('SELECT id,wethaq_id,name,birth_year FROM users WHERE wethaq_id=?').get(String(id||'').trim())}"
          if 'function resolveUser(value)' not in s:s=s.replace(find_old,find_old+"\nfunction resolveUser(value){const q=String(value||'').trim();if(!q)return null;return findUser(q)||db.prepare('SELECT id,wethaq_id,name,birth_year FROM users WHERE name=? COLLATE NOCASE').get(q)}",1)
          s=s.replace("const rows=db.prepare('SELECT ar.role,u.id,u.wethaq_id,u.name,u.birth_year FROM admin_roles ar JOIN users u ON u.id=ar.user_id ORDER BY ar.id').all();","const rows=db.prepare('SELECT ar.role,ar.expires_at,u.id,u.wethaq_id,u.name,u.birth_year FROM admin_roles ar JOIN users u ON u.id=ar.user_id ORDER BY ar.id').all();",1)
          s=s.replace("const target=findUser(req.body?.wethaqId),role=String(req.body?.role||'');","const target=resolveUser(req.body?.wethaqId||req.body?.target),role=String(req.body?.role||'');",1)
          ins_old="const code=newAdminCode();db.prepare('INSERT INTO admin_roles(user_id,role,admin_code_hash,created_by) VALUES(?,?,?,?)').run(target.id,role,hashCode(code),req.user.sub);"
          ins_new="const code=newAdminCode(),expiresAt=new Date(Date.now()+365*24*60*60*1000).toISOString();db.prepare('INSERT INTO admin_roles(user_id,role,admin_code_hash,created_by,expires_at) VALUES(?,?,?,?,?)').run(target.id,role,hashCode(code),req.user.sub,expiresAt);"
          s=s.replace(ins_old,ins_new,1)
          s=s.replace("adminEvent(target.id,{type:'role_assigned',role,role_label:roleLabel(role),message:","adminEvent(target.id,{type:'role_assigned',role,role_label:roleLabel(role),expires_at:expiresAt,message:",1)
          s=s.replace("res.status(201).json({ok:true,user:target,role,role_label:roleLabel(role),admin_code:code})","res.status(201).json({ok:true,user:target,role,role_label:roleLabel(role),admin_code:code,expires_at:expiresAt})",1)
          verify_old="const code=String(req.body?.code||'').trim().toUpperCase(),row=db.prepare('SELECT role,admin_code_hash FROM admin_roles WHERE user_id=?').get(req.user.sub);if(!row||!code||hashCode(code)!==row.admin_code_hash)return res.status(403).json({error:'invalid_admin_code'});"
          verify_new="const code=String(req.body?.code||'').trim().toUpperCase(),row=db.prepare('SELECT role,admin_code_hash,expires_at FROM admin_roles WHERE user_id=?').get(req.user.sub);if(!row||!code||hashCode(code)!==row.admin_code_hash)return res.status(403).json({error:'invalid_admin_code'});if(row.expires_at&&new Date(row.expires_at)<=new Date())return res.status(403).json({error:'admin_role_expired'});"
          s=s.replace(verify_old,verify_new,1)
          s=s.replace("const target=findUser(req.body?.wethaqId),minutes=","const target=resolveUser(req.body?.wethaqId||req.body?.target),minutes=",2)
          s=s.replace("const target=findUser(req.body?.wethaqId),reason=","const target=resolveUser(req.body?.wethaqId||req.body?.target),reason=",1)
          s=s.replace("const u=findUser(req.body?.wethaqId);if(!u)return res.status(404).json({error:'user_not_found'});if(u.wethaq_id===OWNER_WETHAQ_ID)return res.status(400).json({error:'cannot_ban_owner'});","const u=resolveUser(req.body?.wethaqId||req.body?.target);if(!u)return res.status(404).json({error:'user_not_found'});if(u.wethaq_id===OWNER_WETHAQ_ID)return res.status(403).json({error:'cannot_ban_owner'});",1)
          W(p,s)

          # Incoming call reply/reject actions.
          p='app/src/main/java/com/wethaq/app/WethaqMessageService.java';s=R(p)
          if 'addAction(new NotificationCompat.Action.Builder' not in s:
              marker='private void showIncomingCall('
              st=s.find(marker)
              if st<0: raise SystemExit('PATCH_MISSING: incoming call method')
              br=s.find('{',st);depth=0;en=None
              for i in range(br,len(s)):
                  if s[i]=='{':depth+=1
                  elif s[i]=='}':
                      depth-=1
                      if depth==0:en=i+1;break
              old=s[st:en]
              new='''private void showIncomingCall(String target,String name,boolean audioOnly,String offerPayload){Intent i=new Intent(this,VideoCallActivity.class);i.putExtra("target",target);i.putExtra("name",name);i.putExtra("audioOnly",audioOnly);i.putExtra("incomingOffer",offerPayload);i.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK|Intent.FLAG_ACTIVITY_CLEAR_TOP|Intent.FLAG_ACTIVITY_SINGLE_TOP);PendingIntent accept=PendingIntent.getActivity(this,(target+name+"accept").hashCode(),i,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Intent rejectIntent=new Intent(this,IncomingCallActionReceiver.class).setAction("com.wethaq.REJECT_CALL").putExtra("target",target).putExtra("token",getSharedPreferences("wethaq",MODE_PRIVATE).getString("token",""));PendingIntent reject=PendingIntent.getBroadcast(this,(target+name+"reject").hashCode(),rejectIntent,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);NotificationCompat.Builder b=new NotificationCompat.Builder(this,CALL_CHANNEL).setSmallIcon(android.R.drawable.sym_call_incoming).setContentTitle((audioOnly?"مكالمة صوتية واردة من ":"مكالمة فيديو واردة من ")+name).setContentText("يمكنك الرد أو الرفض مباشرة").setContentIntent(accept).setFullScreenIntent(accept,true).addAction(new NotificationCompat.Action.Builder(android.R.drawable.ic_menu_call,"رد",accept).build()).addAction(new NotificationCompat.Action.Builder(android.R.drawable.ic_menu_close_clear_cancel,"رفض",reject).build()).setAutoCancel(true).setPriority(NotificationCompat.PRIORITY_MAX).setCategory(NotificationCompat.CATEGORY_CALL).setOngoing(true).setTimeoutAfter(60000);NotificationManager nm=getSystemService(NotificationManager.class);if(nm!=null)nm.notify(4102,b.build());}'''
              s=s[:st]+new+s[en:];W(p,s)
          rp='app/src/main/java/com/wethaq/app/IncomingCallActionReceiver.java'
          if not Path(rp).exists():W(rp,'''package com.wethaq.app;import android.content.BroadcastReceiver;import android.content.Context;import android.content.Intent;import java.io.OutputStream;import java.net.HttpURLConnection;import java.net.URL;import java.nio.charset.StandardCharsets;public final class IncomingCallActionReceiver extends BroadcastReceiver{private static final String API="https://wethaq-backend-production.up.railway.app";@Override public void onReceive(Context c,Intent i){if(!"com.wethaq.REJECT_CALL".equals(i.getAction()))return;String target=i.getStringExtra("target"),token=i.getStringExtra("token");if(target==null||token==null)return;new Thread(()->{HttpURLConnection h=null;try{h=(HttpURLConnection)new URL(API+"/api/calls/signal").openConnection();h.setRequestMethod("POST");h.setDoOutput(true);h.setRequestProperty("Authorization","Bearer "+token);h.setRequestProperty("Content-Type","application/json");String body=new org.json.JSONObject().put("to",target).put("type","end").put("payload","{}").toString();try(OutputStream out=h.getOutputStream()){out.write(body.getBytes(StandardCharsets.UTF_8));}h.getResponseCode();}catch(Exception ignored){}finally{if(h!=null)h.disconnect();}}).start();}}\n''')
          mp='app/src/main/AndroidManifest.xml';m=R(mp)
          if 'IncomingCallActionReceiver' not in m:W(mp,m.replace('</application>','<receiver android:name=".IncomingCallActionReceiver" android:exported="false"/>\n    </application>'))

          # Audio device routing.
          p='app/src/main/java/com/wethaq/app/VideoCallActivity.java';s=R(p)
          if 'AudioDeviceInfo' not in s:s=s.replace('import android.media.AudioManager;','import android.media.AudioManager;import android.media.AudioDeviceInfo;import android.media.AudioFocusRequest;',1)
          if 'AudioFocusRequest audioFocusRequest' not in s:s=s.replace('private AudioManager audioManager;','private AudioManager audioManager;private AudioFocusRequest audioFocusRequest;',1)
          if 'private void applyAudioRoute()' not in s:
              s=s.replace('    private void toggleMute(){','    private void applyAudioRoute(){if(audioManager==null)return;if(Build.VERSION.SDK_INT>=31){AudioDeviceInfo wanted=null;for(AudioDeviceInfo d:audioManager.getAvailableCommunicationDevices()){if(speakerOn&&d.getType()==AudioDeviceInfo.TYPE_BUILTIN_SPEAKER){wanted=d;break;}if(!speakerOn&&d.getType()==AudioDeviceInfo.TYPE_BUILTIN_EARPIECE){wanted=d;break;}}if(wanted!=null)audioManager.setCommunicationDevice(wanted);}else{audioManager.setSpeakerphoneOn(speakerOn);}}\n    private void toggleMute(){',1)
          s=s.replace('audioManager.setSpeakerphoneOn(speakerOn);speakerButton.setText(speakerOn?"🔊 السماعة: تشغيل":"🔈 السماعة: إيقاف");','speakerOn=!speakerOn;applyAudioRoute();speakerButton.setText(speakerOn?"🔊 السماعة: تشغيل":"🔈 السماعة: إيقاف");',1)
          s=s.replace('audioManager.setMode(AudioManager.MODE_IN_COMMUNICATION);audioManager.setSpeakerphoneOn(true);speakerOn=true;','audioManager.setMode(AudioManager.MODE_IN_COMMUNICATION);if(Build.VERSION.SDK_INT>=26){audioFocusRequest=new AudioFocusRequest.Builder(AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_EXCLUSIVE).setOnAudioFocusChangeListener(v->{}).build();audioManager.requestAudioFocus(audioFocusRequest);}speakerOn=true;applyAudioRoute();',1)
          # Guard accidental double toggle from previous replacement.
          s=s.replace('speakerOn=!speakerOn;speakerOn=!speakerOn;applyAudioRoute();','speakerOn=!speakerOn;applyAudioRoute();')
          W(p,s)

          print('WETHAQ_V4_SOURCE_REPAIRS_READY')
          PY

      - name: Validate source and backend
        run: |
          git diff --check
          node --check backend/server.js
          grep -R "20\\$ / سنة\|17\\$ / سنة\|12\\$ / سنة\|8\\$ / سنة\|المناصب الإدارية مدفوعة\|addModerationTargetCard\|applyAudioRoute\|\"رد\"\|expires_at\|resolveUser" app/src/main/java backend/server.js >/dev/null

      - name: Backend smoke with hard health gate
        working-directory: backend
        shell: bash
        run: |
          npm ci
          export DB_PATH="$RUNNER_TEMP/wethaq-v4.db"
          export JWT_SECRET="wethaq-v4-secret"
          export OWNER_WETHAQ_ID="Hatem_Hussin_Al_Haj_Ramadan1995"
          export PORT=3199
          node server.js > "$RUNNER_TEMP/wethaq-v4-server.log" 2>&1 &
          PID=$!
          trap 'kill $PID 2>/dev/null || true' EXIT
          python3 - <<'PY'
          import json,time,urllib.request
          for _ in range(60):
              try:
                  with urllib.request.urlopen('http://127.0.0.1:3199/health',timeout=1) as r:
                      d=json.loads(r.read().decode())
                      if r.status==200 and d.get('ok') is True: print('HEALTH_OK');break
              except Exception: pass
              time.sleep(.25)
          else:
              print(open('/runner/_temp/wethaq-v4-server.log').read());raise SystemExit('HEALTH_GATE_FAILED')
          PY
          node smoke-test.mjs

      - name: Android Release build
        run: ./gradlew --no-daemon clean assembleRelease

      - name: Commit repaired source
        shell: bash
        run: |
          git diff --check
          git config user.name 'wethaq-ci'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/com/wethaq/app/MainActivity.java app/src/main/java/com/wethaq/app/AdminActivity.java app/src/main/java/com/wethaq/app/PublicAdministrationActivity.java app/src/main/java/com/wethaq/app/WethaqMessageService.java app/src/main/java/com/wethaq/app/IncomingCallActionReceiver.java app/src/main/java/com/wethaq/app/VideoCallActivity.java app/src/main/AndroidManifest.xml backend/server.js
          if git diff --cached --quiet; then echo 'NO_SOURCE_DIFF'; exit 0; fi
          git commit -m 'feat: complete Wethaq V4 UI RBAC and call repairs'
          git push origin HEAD:main

      - name: Upload verified release
        uses: actions/upload-artifact@v4
        with:
          name: wethaq-v4-repaired-release
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error
          retention-days: 30
