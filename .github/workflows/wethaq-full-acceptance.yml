name: Wethaq Full Acceptance Validation

on:
  workflow_dispatch:

jobs:
  disabled-legacy-path:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy acceptance path disabled. Canonical production verification is managed by Wethaq Canonical Production Gate.'
