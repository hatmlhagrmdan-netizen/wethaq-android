name: Wethaq Finalize Visual Admin

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy visual-admin finalizer disabled; canonical production gate owns source repair and release build.'
