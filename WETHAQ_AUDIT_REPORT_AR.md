# تقرير الفحص والاختبار الشامل لتطبيق وَثاق

**المستودع:** `hatmlhagrmdan-netizen/wethaq-android`
**الفرع/الالتزام المفحوص:** `main` / `1fbd1a0d9b26d1b7001cabedbbf9622edae6c46d`
**نوع الفحص:** قراءة وتحليل وتشغيل اختبارات محلية؛ لم يتم تعديل المستودع أو تنفيذ كتابة على GitHub.
**النتيجة العامة:** الخادم ومسارات الاختبار الأساسية تعمل، لكن المستودع غير جاهز للاعتماد النهائي قبل معالجة مخاطر الهوية والأسرار، توحيد الإصدارات والتوثيق، وإكمال اختبار Android الحقيقي على أجهزة/محاكي.

## 1. نطاق الفحص والمنهجية

تم تنفيذ الآتي:

1. استنساخ المستودع بآخر نسخة من `main`.
2. فحص ملفات البناء، Manifest، الموارد، Java، Node.js، CI، والوثائق.
3. تشغيل التدقيق المضمن `scripts/mainline_stable_audit.py`.
4. فحص الصياغة بواسطة `node --check` و`python3 -m py_compile`.
5. تثبيت اعتماديات الخادم وتشغيل:
   - `runtime-hardening.mjs`
   - `security-smoke-test.mjs`
   - `smoke-test.mjs`
   - `hardening-smoke.mjs` على قاعدة بيانات معزولة.
6. تشغيل `npm audit --omit=dev --audit-level=high`.
7. اختبار فعلي لإعداد CORS وتسريب الصورة في البحث العام.
8. مراجعة آخر GitHub Actions للالتزام المفحوص.

## 2. نتائج الاختبارات

| الفحص | النتيجة | الملاحظات |
|---|---:|---|
| التدقيق المضمن | ناجح | `MAINLINE_STABLE_AUDIT_OK` |
| فحص JavaScript/Python | ناجح | لم تظهر أخطاء صياغة |
| اختبار صحة الخادم | ناجح | `/health` أعاد `ok: true` |
| اختبار الأمان | ناجح | `WETHAQ_SECURITY_SMOKE_OK` |
| الاختبار الوظيفي | ناجح | `WETHAQ_SMOKE_TEST_OK`؛ هوية، دخول، بحث، رسائل، WebSocket، إشارات مكالمات وسجل المكالمات |
| اختبار التقوية | ناجح عند العزل | `WETHAQ_HARDENING_SMOKE_OK` |
| تدقيق الاعتماديات | ناجح | `found 0 vulnerabilities` على مستوى high |
| CI البعيد للالتزام الحالي | ناجح | Validation وProtected Production APK كلاهما `success` |
| بناء Android محليًا | غير منفذ | البيئة لا تحتوي Gradle أو Android SDK؛ تم الاعتماد على CI البعيد كدليل بناء |
| اختبار مكالمة صوت/فيديو حقيقية بين جهازين | غير مكتمل | الاختبار المحلي تحقق من signaling فقط، لا من media end-to-end |

> فشل اختبار التقوية في المحاولة الأولى كان بسبب تشغيله على نفس قاعدة بيانات اختباري `smoke` التي أنشأت هوية المؤسس مسبقًا. عند تشغيله كما في CI الرسمي على قاعدة منفصلة نجح بالكامل؛ لا أعدّه عيبًا في المنتج.

## 3. المشكلات ذات الأولوية العالية

### H1 — رموز الدخول الشخصية لا تخضع لمحدد محاولات

**الدليل:** `backend/server.js` يطبق `rateLimit()` في مسار `/api/admin/login` فقط، بينما `/api/identity` و`/api/login` يقبلان رمزًا شخصيًا من 6 إلى 12 رقمًا دون محدد محاولات ظاهر.

**الأثر:** يمكن تنفيذ تخمين آلي لرمز مكون من ستة أرقام، خصوصًا مع معرفة الاسم وسنة الميلاد أو قابلية البحث العام عن المستخدمين.

**المعالجة المقترحة:**

- إضافة rate limit موزون على `(IP + الهوية)` لمساري الهوية وتسجيل الدخول.
- إبطاء تدريجي بعد المحاولات الفاشلة، وقفل مؤقت بعد حد منخفض.
- تسجيل محاولات الفشل والتنبيه عند الأنماط غير الطبيعية.
- عدم الاعتماد على `req.ip` فقط خلف Proxy إلا بعد ضبط `trust proxy` بقائمة موثوقة.
- التفكير في رمز أطول أو عامل جهاز/تحدٍّ إضافي عند تسجيل جهاز جديد.

### H2 — تخزين JWT ورموز الإدارة في SharedPreferences العادية

**الدليل:** `MainActivity.java` و`AdminAccessActivity.java` يستخدمان `getSharedPreferences("wethaq", MODE_PRIVATE)` لحفظ `token` و`admin_token`.

**الأثر:** حماية `MODE_PRIVATE` ليست تشفيرًا. في جهاز مخترق/نسخة Debug أو نسخ احتياطي غير متوقع قد تتعرض جلسة المستخدم أو جلسة الإدارة.

**المعالجة المقترحة:**

- استخدام Android Keystore مع `EncryptedSharedPreferences` أو تخزين رمز قصير العمر داخل Keystore.
- فصل تخزين جلسة المستخدم عن جلسة الإدارة.
- مسح الرموز عند انتهاء الصلاحية أو عند `401`، وليس فقط عند تسجيل الخروج.
- إضافة تدوير/إبطال للخادم بدل الاعتماد على JWT طويل العمر فقط.

### H3 — JWT المستخدم صالح لمدة 365 يومًا

**الدليل:** `backend/server.js`: `tokenFor()` ينشئ JWT بـ `expiresIn: '365d'`، ورمز الإدارة بـ `30d`.

**الأثر:** سرقة رمز واحد تمنح دخولًا طويلًا، ولا يوجد إبطال مركزي واضح لجلسة المستخدم العادية.

**المعالجة المقترحة:** access token قصير العمر، refresh token قابل للإبطال والتدوير، جدول جلسات/أجهزة، وإبطال كل الجلسات عند تغيير بيانات حساسة أو حظر الحساب.

### H4 — رمز WebSocket يمر في Query String

**الدليل:** Android يفتح `WethaqConfig.WS + "?token=" + encoded`، والخادم يقرأ `request.url`.

**الأثر:** Query strings قد تظهر في سجلات Reverse Proxy أو أدوات المراقبة أو آثار الشبكة.

**المعالجة المقترحة:** مصادقة WebSocket عبر `Authorization` أثناء handshake إن كانت البنية تدعمها، أو تذكرة قصيرة العمر أحادية الاستخدام بدل JWT طويل العمر.

### H5 — البحث العام يسرّب Base64 للصورة الشخصية

**التحقق العملي:** أنشأت هوية اختبار، رفعت صورة Base64، ثم نفذت طلبًا غير مصادق إلى `/api/search`. النتيجة تضمنت:

```json
{"has_avatar":true,"has_avatar_data":true,"avatar_data_length":8}
```

**الدليل البرمجي:** `/api/search` يستخدم `publicUser(u, true)`، و`publicUser(..., true)` يضمّن `avatar_data`.

**الأثر:** كشف بيانات الصورة للمستخدم غير المصادق وزيادة حجم استجابات البحث واستهلاك الذاكرة/النطاق الترددي.

**المعالجة المقترحة:** جعل البحث يعيد `has_avatar` فقط، والإبقاء على الصورة في endpoint منفصل مصادق عليه أو استخدام صور مصغرة/CDN مع صلاحيات.

## 4. مشكلات متوسطة ومخاطر تشغيلية

### M1 — تناقض إعداد CORS في الوثائق والكود

`backend/.env.example` يعرّف `CORS_ORIGIN=*`، بينما `backend/server.js` يقرأ `ALLOWED_ORIGINS` فقط.

**التحقق العملي:** عند تشغيل `CORS_ORIGIN=*` لم يظهر `Access-Control-Allow-Origin`، وعند تشغيل `ALLOWED_ORIGINS=https://example.com` ظهر العنوان كما هو متوقع.

**المعالجة:** توحيد الاسم، ويفضل قائمة Origins صريحة بدل `*`، مع تحديث المثال والـDeployment Guide.

### M2 — التخزين قد يكون مؤقتًا وفقدان البيانات غير مراقب

`/health` يعلن `storage: local_or_ephemeral` عند غياب `RAILWAY_VOLUME_MOUNT_PATH`، وقاعدة SQLite محلية. لا توجد خطة Backup/Restore أو اختبار استعادة داخل المستودع.

**المعالجة:** فرض Volume في الإنتاج، رفض بدء التشغيل الإنتاجي عند غيابه، نسخ احتياطي مشفر دوري، اختبار استعادة، ومراقبة مساحة القرص وWAL.

### M3 — لا يوجد TURN مضبوط في المثال

`/api/calls/ice-config` يملك STUN افتراضيًا، ويضيف TURN فقط عند ضبط متغيراته. `.env.example` لا يوضح متغيرات TURN.

**الأثر:** المكالمات قد تعمل على شبكات بسيطة وتفشل خلف NAT/شبكات شركات/شبكات جوال معينة.

**المعالجة:** توفير TURN إنتاجي موثوق ببيانات مؤقتة، تدوير الاعتماديات، وقياس نجاح المكالمات حسب الشبكة والجهاز.

### M4 — الاعتماديات غير قابلة لإعادة الإنتاج بالكامل

ملف `backend/package-lock.json` غير متتبع في Git، و`gradle-wrapper` غير موجود. كما أن `engines.node` يطلب نسخة دقيقة `22.23.2` بينما بيئة الفحص كانت `22.13.0` وأصدرت `EBADENGINE`.

**المعالجة:** تتبع lockfile، إضافة Gradle Wrapper، استخدام نطاق Node متوافق مثل `>=22.13 <23` أو تثبيت نسخة CI بوضوح، وتوحيد Node محليًا وCI.

### M5 — الـCI يعرّف سر بصمة الشهادة ثم يتجاهله

في `.github/workflows/finalize-v2.yml` يتم تمرير `WETHAQ_CERT_SHA256`، لكن المقارنة تستخدم بصمة حرفية ثابتة داخل workflow (`EXPECTED=...`).

**الأثر:** تدوير شهادة الإنتاج يتطلب تعديل workflow، وقد يسبب نجاحًا/فشلًا غير متوقع عند تغيير الشهادة.

**المعالجة:** استخدام السر الممرر بعد تطبيع الصيغة، أو جعل البصمة جزءًا موثقًا ومراجعًا من مصدر الإصدار، مع منع وجود قيم متعارضة.

### M6 — الحماية من التسريب تعتمد على اختبار محدود

اختبار الأمان يتحقق من عدم وجود `personal_code_hash` و`birth_year` في البحث، لكنه لم يمنع تسريب `avatar_data` الذي ظهر في الاختبار العملي.

**المعالجة:** إضافة assertions للصور والحقول الحساسة الأخرى، واختبار مصادقة/خصوصية لكل endpoint، وليس فقط اختبار happy path.

## 5. تناقضات الإصدارات والوثائق

وجدت عدة مصادر للحقيقة:

- `app/build.gradle`: `versionName 2.5.0` و`versionCode 9`.
- `README.md`: يذكر Android `2.4.0` و`versionCode 8`.
- `/health`: يعلن نسخة خادم `6.0.2`.
- `backend/package.json`: نسخة `4.2.2`.
- `package.json` الجذري: نسخة `1.0.2`.
- `PRODUCTION_BUILD_MANIFEST.md` و`DEPLOYMENT_GUIDE.md`: ما زالا يتحدثان عن `v1.0.0` و`server-v7.js` وWorkflow باسم `wethaq-release-verified`، بينما الملف الفعلي هو `finalize-v2.yml` والمشغل الفعلي `production-start.mjs`.
- `DEPLOYMENT_GUIDE.md` يذكر السر `WETHAQ_KEYSTORE_BASE64`، بينما الـCI الفعلي يستخدم `WETHAQ_PRODUCTION_KEYSTORE_B64`.

**الأثر:** خطر بناء أو نشر نسخة مختلفة عن النسخة التي راجعها الفريق، وصعوبة تتبع APK/ AAB إلى المصدر.

**المعالجة:** اعتماد مصدر واحد للإصدار، تحديث README والوثائق، إضافة `BuildInfo` مولّد تلقائيًا من CI، تضمين commit SHA وversionName وversionCode داخل التطبيق، وربط كل Artifact بتقرير Provenance.

## 6. ملاحظات الأداء وقابلية التوسع

1. **الخادم في ملف واحد شبه مضغوط** (`backend/server.js` نحو 50KB/93 سطرًا فعليًا)، ما يصعّب المراجعة والاختبار والتطوير. تقسيمه إلى routes/services/repositories/middleware سيقلل مخاطر التعديل.
2. البحث يستخدم `LIKE '%query%'` على الاسم والمعرف. مع نمو البيانات سيصبح مكلفًا؛ استخدم FTS5 أو فهرسًا مناسبًا مع prefix search وحدود/صفحات.
3. الرسائل والصور والصوت مخزنة كـBase64 داخل SQLite؛ هذا يضخم DB والذاكرة والاستجابات. الأفضل object storage مع metadata في SQLite، وتحجيم/ضغط الصور قبل الرفع.
4. الاحتفاظ بقائمة `attempts` في الذاكرة لا يعمل عبر أكثر من instance ولا يملك تنظيفًا دوريًا للمفاتيح القديمة؛ استخدم Redis أو rate limiter موزعًا.
5. Android ينشئ واجهات كبيرة برمجيًا، و`MainActivity.java` نحو 48KB؛ فصل ViewModel/Repository/واجهات أو مكونات أصغر سيحسن دورة الحياة والاختبار.
6. إعادة رسم الرسائل عبر `removeAllViews()` مع عرض آخر 60 رسالة مناسب كبداية لكنه ليس RecyclerView؛ استخدم RecyclerView مع diff/paging لتقليل التقطيع.
7. توجد عمليات شبكة يدوية متعددة عبر `HttpURLConnection` وOkHttp؛ توحيد طبقة الشبكة مع interceptors وtimeouts وretry policy سيقلل التكرار والأخطاء.
8. توجد عمليات `catch(Exception ignored)` كثيرة في مسارات الرسائل والمكالمات؛ أضف logging آمنًا/telemetry قابلًا للتحكم حتى لا تتحول الأعطال الصامتة إلى فقدان رسائل.

## 7. ملاحظات الواجهة والتنسيق وإمكانية الوصول

- أغلب الواجهة مبنية برمجيًا ولا توجد ملفات layout XML تقريبًا؛ هذا يسرّع النمو الأولي لكنه يصعّب التصميم المتجاوب، الاختبار البصري، والترجمة.
- لا تظهر استخدامات واضحة لـ`contentDescription` لعناصر الصور والأيقونات؛ يلزم تحسين TalkBack وإتاحة الوصول.
- توجد أبعاد ثابتة كثيرة مثل `dp(76)`, `dp(260)` وأحجام نص متعددة داخل Java؛ قد تظهر مشاكل على الشاشات الصغيرة أو إعدادات الخط الكبير.
- بعض الأزرار تعتمد على رموز Unicode/Emoji مثل الهاتف والميكروفون والصور؛ يلزم توفير icons وcontent descriptions وحالات تباين/ضغط واضحة.
- يجب اختبار RTL، الخطوط الكبيرة، الوضع الليلي، تدوير الشاشة/استعادة الحالة، وإعدادات تقليل الحركة.
- يفضل نقل النصوص إلى `strings.xml` لتوحيد العربية والإنجليزية ودعم الترجمة والتدقيق.

## 8. ما لم يتم إثباته بعد

هذه البنود لا يمكن اعتبارها ناجحة من الاختبارات الحالية:

- مكالمة صوتية حقيقية بين جهازين مع NAT مختلف وTURN.
- مكالمة فيديو حقيقية مع تبديل الكاميرا، الخلفية، انقطاع الشبكة، واستئنافها.
- وصول الإشعارات بعد إعادة تشغيل الجهاز على Android 13–16 مع قيود البطارية.
- اختبار تثبيت APK/AAB على أجهزة فعلية ومراجعة سلوك الأذونات.
- اختبار ضغط وتزامن لعدة مستخدمين ورسائل ورفع وسائط.
- اختبار backup/restore وفقدان/امتلاء Volume.
- اختبار penetration خارجي على بيئة staging مع بيانات غير حقيقية.

## 9. خطة إصلاح مرتبة

### قبل أي إصدار عام

1. إضافة rate limiting وقفل تدريجي لتسجيل الدخول والهوية.
2. إيقاف تضمين `avatar_data` في البحث العام.
3. استبدال SharedPreferences العادية بتخزين مشفر وإضافة إبطال/تدوير للجلسات.
4. توحيد CORS وإعدادات الإنتاج، وفرض Volume وJWT_SECRET وTURN في production.
5. تحديث الوثائق والإصدارات ومصدر الحقيقة.
6. تتبع `package-lock.json` وإضافة Gradle Wrapper.
7. إصلاح استخدام `WETHAQ_CERT_SHA256` في CI.

### قبل توسيع الاستخدام

1. اختبار جهازين فعليين ومصفوفة Android 10–16 وشبكات مختلفة.
2. إضافة اختبار خصوصية للبحث والصور والرسائل والإشعارات.
3. إضافة اختبارات ضغط وقياس p95 للبحث والرسائل والـWebSocket.
4. نقل الوسائط إلى تخزين كائني، وإضافة thumbnails وحدود MIME/أبعاد فعلية.
5. تقسيم الخادم وطبقة Android وتحسين التسجيل والمراقبة.
6. اعتماد نشر staging ثم canary مع rollback ونسخ احتياطي قابل للاستعادة.

## 10. الخلاصة

**وَثاق ليس في حالة فشل شامل:** اختبارات الخادم الموجودة جيدة نسبيًا، وCI البعيد أثبت نجاح بناء Android 16 وتوقيع الإنتاج للالتزام الحالي. لكن النجاح الحالي لا يثبت الجاهزية العامة؛ توجد ثغرة واضحة في تخمين رموز الدخول، وتسريب Base64 للصورة عبر البحث العام، وتخزين جلسات غير مشفر، وتناقضات توثيق وإصدار قد تسبب نشر نسخة خاطئة. الأولوية هي معالجة H1–H5 ثم إعادة تشغيل CI واختبارات الأجهزة الحقيقية قبل إعلان الإصدار.
